/// JanusShared — shared types for the Janus local AI inference network.
///
/// This module contains protocol message types, cryptographic primitives,
/// and domain models used by the provider, client, and backend.
/// It has zero platform dependencies (no UIKit, no AppKit, no Vapor).
public enum JanusShared {
    public static let version = "0.1.0"
}
